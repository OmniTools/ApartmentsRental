<script>
    $(function ( ) {

        $('#proposals').change(function ( ) {

            $('#title').val($(this).val());

        });
    });
</script>

<form method="post" action="{{ controller.getActionUri('ajaxUpdate', { seasonId: season.getId()}) }}" class="ajax">

    <div class="modal-body">

        <div class="form-group">
            <label for="title">Titel</label>
            <input type="text" class="form-control" id="title" name="season[title]" placeholder="Titel" value="{{ season.getTitle() }}">
        </div>
        <div class="form-group">
            <label for="dateFrom">Datum von</label>
            <input type="date" class="form-control" id="dateFrom" name="season[dateFrom]" value="{{ season.getDateFrom().format('Y-m-d') }}">
        </div>
        <div class="form-group">
            <label for="dateTo">Datum bis</label>
            <input type="date" class="form-control" id="dateTo" name="season[dateTo]" value="{{ season.getDateTo().format('Y-m-d') }}">
        </div>
        <div class="form-group">
            <label for="minNights">min. Nächte</label>
            <input type="number" step="1" class="form-control" id="minNights" aria-describedby="minNightsHelp" name="season[minNights]" placeholder="min. Nächte" value="{{ season.getMinNights() }}">
            <small id="minNightsHelp" class="form-text text-muted">Mindestanzahl an zu buchenden Nächsten am Stück in dieser Saison.</small>
        </div>

    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary">Speichern</button>
    </div>

</form>